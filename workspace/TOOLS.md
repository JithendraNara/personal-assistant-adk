# Tool Notes

## API Status
- Web search: Requires SERPAPI_KEY (falls back to mock)
- Finance: Requires ALPHA_VANTAGE_KEY (falls back to mock)
- Sports: Requires SPORTS_API_KEY (falls back to mock)
- Weather: Requires OPENWEATHER_KEY (falls back to mock)

## MCP Servers (Optional)
- Filesystem: npx @modelcontextprotocol/server-filesystem
- Google Maps: npx @modelcontextprotocol/server-google-maps (requires GOOGLE_MAPS_API_KEY)
